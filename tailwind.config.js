/** @type {import('tailwindcss').Config} */
module.exports = {
  // Specify the paths to all of your template files
  content: ["./src/**/*.{js,jsx}"],
  
  // Customize your theme here
  theme: {
    extend: {
      // You can add custom colors, fonts, spacings, etc.
    },
  },
  
  // Add any Tailwind plugins you might be using (none for now)
  plugins: [],
};
